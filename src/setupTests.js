// This line imports the '@testing-library/jest-dom' library, which provides additional matchers and utilities
// for testing with Jest in combination with the DOM (Document Object Model).
// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// Read more from: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';

